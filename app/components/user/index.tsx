'use client'

export default function UserInfo() {

    return (
        <div className='text-cyan-50 flex flex-col p-10 text-2xl'>
            {/* <p>{isLongin? user.userData.name : '還迎來到 Manage'}</p> */}
        </div>
    )
}